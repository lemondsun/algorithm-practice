/**
 * .sort() method
 * accepts a comparator function
 * tells JS how to sort
 * looks at pairs of elements (a and b) and determines the sort order based on the return
 * if negative, a before b
 * if positive, a after b
 */